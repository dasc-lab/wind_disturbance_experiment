# Training and Testing Gaussian Process Models
1. Run npy_files to truncate the takeoff and landing data from every trajectory and concatenate the results together to `disturbance.npy` and `input.npy`
